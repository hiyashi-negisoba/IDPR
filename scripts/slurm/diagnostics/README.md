# Diagnostic Slurm jobs

These jobs reproduce fixed smoke and historical flat-card comparisons. They are not
production entry points. The general full-dataset runner is
`scripts/slurm/run_issue_pipeline_batch.sh`.
