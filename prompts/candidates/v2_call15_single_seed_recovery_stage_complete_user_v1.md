아래 INPUT_JSON의 단일 seed에 factual candidate binding이 있는지 actor별로 다시 확인하라.
완성된 정범 실행뿐 아니라 원문에 명시된 준비·실행착수·요청·지시·도움·부작위도 검토하되,
법적 형태나 성립 여부는 판단하지 않는다. actor 본인의 행위와 함께 볼 context를 구분하고,
모든 quote를 factual_scope_text에서 정확히 복사하라. 설명 없이 JSON 객체 하나만 출력하라.

INPUT_JSON:
{{INPUT_JSON}}
