아래 INPUT_JSON의 단 하나의 unbound seed에 대해 candidate actor 본인의 direct-conduct factual
episode만 복구하라. 결과 미발생만으로 준비·착수 candidate를 버리지 말되, 타인에 대한 요청·지시·
도움만 있는 actor는 direct binding으로 만들지 마라. 모든 quote는 factual_scope_text 안의 exact
substring이어야 한다. 설명 없이 JSON 객체 하나만 출력하라.

<INPUT_JSON>
{{INPUT_JSON}}
</INPUT_JSON>
