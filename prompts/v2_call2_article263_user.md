아래 INPUT_JSON의 `article263_pairs`를 입력 순서대로 평가하라. 일반 occurrence catalog와
`occurrence_evidence` 및 각 pair의 `relation_evidence`만 사용하라. 법정의제 세 truth는
`statutory_truths`, shared injury result 한 truth는 `shared_result_truths`에 출력하라.

<INPUT_JSON>
{{INPUT_JSON}}
</INPUT_JSON>
