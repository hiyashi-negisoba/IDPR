아래 INPUT_JSON에서 형법 실체법 쟁점을 폐쇄형 unit으로 식별하라. `allowed_units`는 검색 결과가
아니라 실행 가능한 전체 allowlist다. 등록되지 않은 중요한 총칙 쟁점은 `unsupported`로 보존하고,
절차법 쟁점은 제외하라.

<INPUT_JSON>
{{INPUT_JSON}}
</INPUT_JSON>

JSON 객체 하나만 출력하라.
