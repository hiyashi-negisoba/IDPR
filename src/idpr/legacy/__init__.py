"""Archived pilot implementations kept solely for reproducibility.

The active, offence-agnostic pipeline lives in ``idpr.neural`` submodules,
``idpr.issue_pipeline``, and ``idpr.generation.issue_answer``.
"""
