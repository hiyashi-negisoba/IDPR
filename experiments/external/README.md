# External benchmark evaluation

This directory contains adapters and normalized artifacts for external evaluation.

- lbox_call1: Call 1 DefinitionRef-level routing evaluation
- kbl_call2: Call 2 TRUE/FALSE/UNKNOWN atomic assessment evaluation

Dataset adapters must not modify production routing, binding, symbolic runtime, or prompts. They only map external annotations into the evaluation contract.
